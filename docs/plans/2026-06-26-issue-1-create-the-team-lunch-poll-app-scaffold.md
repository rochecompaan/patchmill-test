# Team Lunch Poll App Scaffold Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the initial local Team Lunch Poll web app scaffold with a static app shell and documented commands.

**Architecture:** Use a small Vite + React + TypeScript single-page app. The first implementation is entirely static: package/configuration files boot the app, `src/App.tsx` renders the shell, `src/App.css` owns presentation, and README documents how contributors run and check it.

**Tech Stack:** Node.js/npm, Vite, React, TypeScript, CSS.

## Global Constraints

- `AGENTS.md` is not present in this repository, so use the spec's verification strategy and the commands introduced in this plan.
- Keep the first scaffold local, lightweight, and easy to run.
- Include a clear `Team Lunch Poll` heading.
- Include a short product description explaining that the app helps teammates choose a lunch option together.
- Include a visually distinct placeholder area for future poll creation and results UI.
- Do not persist data, accept poll input, record votes, calculate winners, call a backend, require login, add authentication, add backend services, or add deployment setup.
- Document commands to install dependencies, run the app locally, and run checks.
- Validation commands for this issue: `npm install`, `npm run check`, and `npm run dev -- --host 127.0.0.1` followed by browser verification.

---

## File Structure

- Create `package.json`: npm metadata, dependency declarations, and scripts for local development and checks.
- Create `package-lock.json`: generated by `npm install`; commit it so installs are reproducible.
- Create `index.html`: Vite HTML entrypoint with a `root` mount node.
- Create `tsconfig.json`: TypeScript compiler settings for the app.
- Create `tsconfig.node.json`: TypeScript settings for Vite configuration.
- Create `vite.config.ts`: Vite React plugin configuration.
- Create `src/main.tsx`: React root bootstrap that mounts `App`.
- Create `src/App.tsx`: static Team Lunch Poll shell content.
- Create `src/App.css`: app-specific responsive styling.
- Create `src/index.css`: global page/reset styling.
- Modify `README.md`: add contributor commands for install, local development, and checks.

---

### Task 1: Add Vite React TypeScript Scaffold

**Files:**
- Create: `package.json`
- Create: `package-lock.json`
- Create: `index.html`
- Create: `tsconfig.json`
- Create: `tsconfig.node.json`
- Create: `vite.config.ts`
- Create: `src/main.tsx`
- Create: `src/App.tsx`
- Create: `src/App.css`
- Create: `src/index.css`

**Interfaces:**
- Consumes: no application code from earlier tasks.
- Produces: an npm project with scripts `dev`, `build`, `typecheck`, `preview`, and `check`; a React `App(): JSX.Element` component exported as the default export from `src/App.tsx`; and a browser page mounted at `#root`.

- [ ] **Step 1: Create the npm package definition**

Create `package.json` with exactly this content:

```json
{
  "name": "team-lunch-poll",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "vite preview",
    "check": "npm run typecheck && npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "typescript": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest"
  }
}
```

- [ ] **Step 2: Install dependencies and generate the lockfile**

Run:

```bash
npm install
```

Expected: npm creates `package-lock.json`, installs dependencies into ignored `node_modules/`, and exits with code 0.

- [ ] **Step 3: Create the Vite HTML entrypoint**

Create `index.html` with exactly this content:

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Team Lunch Poll</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

- [ ] **Step 4: Create TypeScript configuration files**

Create `tsconfig.json` with exactly this content:

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["DOM", "DOM.Iterable", "ES2020"],
    "allowJs": false,
    "skipLibCheck": true,
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,
    "strict": true,
    "forceConsistentCasingInFileNames": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "noEmit": true,
    "jsx": "react-jsx"
  },
  "include": ["src"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

Create `tsconfig.node.json` with exactly this content:

```json
{
  "compilerOptions": {
    "composite": true,
    "module": "ESNext",
    "moduleResolution": "Node",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 5: Create Vite configuration**

Create `vite.config.ts` with exactly this content:

```ts
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
});
```

- [ ] **Step 6: Create the React bootstrap**

Create `src/main.tsx` with exactly this content:

```tsx
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import './index.css';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

- [ ] **Step 7: Create the static app shell component**

Create `src/App.tsx` with exactly this content:

```tsx
import './App.css';

export default function App() {
  return (
    <main className="app-shell" aria-labelledby="app-title">
      <section className="hero-card">
        <p className="eyebrow">Team decision helper</p>
        <h1 id="app-title">Team Lunch Poll</h1>
        <p className="description">
          Help teammates choose a lunch option together with a simple shared poll.
        </p>
      </section>

      <section className="placeholder-card" aria-labelledby="placeholder-title">
        <div>
          <p className="eyebrow">Coming next</p>
          <h2 id="placeholder-title">Poll creation and live results</h2>
        </div>
        <p>
          This space will hold the lunch poll form, voting options, and current results as the app grows.
        </p>
      </section>
    </main>
  );
}
```

- [ ] **Step 8: Create app-specific styling**

Create `src/App.css` with exactly this content:

```css
.app-shell {
  width: min(100%, 960px);
  display: grid;
  gap: 1.5rem;
}

.hero-card,
.placeholder-card {
  border: 1px solid rgba(15, 23, 42, 0.12);
  border-radius: 28px;
  background: rgba(255, 255, 255, 0.9);
  box-shadow: 0 24px 80px rgba(15, 23, 42, 0.12);
}

.hero-card {
  padding: clamp(2rem, 5vw, 4rem);
}

.placeholder-card {
  display: grid;
  gap: 1rem;
  padding: clamp(1.5rem, 4vw, 2.5rem);
  border-style: dashed;
  background: rgba(255, 251, 235, 0.9);
}

.eyebrow {
  margin: 0 0 0.75rem;
  color: #b45309;
  font-size: 0.78rem;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
}

h1,
h2,
p {
  margin-top: 0;
}

h1 {
  margin-bottom: 1rem;
  color: #0f172a;
  font-size: clamp(2.5rem, 8vw, 5rem);
  line-height: 0.95;
}

h2 {
  margin-bottom: 0;
  color: #1f2937;
  font-size: clamp(1.5rem, 4vw, 2.25rem);
}

.description {
  max-width: 36rem;
  margin-bottom: 0;
  color: #475569;
  font-size: clamp(1.125rem, 2vw, 1.35rem);
  line-height: 1.6;
}

.placeholder-card p:last-child {
  margin-bottom: 0;
  color: #4b5563;
  font-size: 1rem;
  line-height: 1.6;
}
```

- [ ] **Step 9: Create global page styling**

Create `src/index.css` with exactly this content:

```css
:root {
  font-family:
    Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI",
    sans-serif;
  color: #111827;
  background: #f8fafc;
  font-synthesis: none;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}

* {
  box-sizing: border-box;
}

body {
  min-width: 320px;
  min-height: 100vh;
  margin: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: clamp(1rem, 4vw, 3rem);
  background:
    radial-gradient(circle at top left, rgba(251, 191, 36, 0.28), transparent 32rem),
    linear-gradient(135deg, #fff7ed 0%, #f8fafc 48%, #ecfeff 100%);
}

button,
input,
textarea,
select {
  font: inherit;
}
```

- [ ] **Step 10: Run the scaffold check command**

Run:

```bash
npm run check
```

Expected: `npm run typecheck` and `vite build` complete successfully, and the command exits with code 0.

- [ ] **Step 11: Run local app verification**

Run:

```bash
npm run dev -- --host 127.0.0.1
```

Expected: Vite prints a local URL such as `http://127.0.0.1:5173/`. Open that URL and verify the page shows the `Team Lunch Poll` heading, the product description, and the visually distinct placeholder section for poll creation and live results. Stop the dev server with `Ctrl+C` after verification.

- [ ] **Step 12: Commit the scaffold implementation**

Run:

```bash
git add package.json package-lock.json index.html tsconfig.json tsconfig.node.json vite.config.ts src/main.tsx src/App.tsx src/App.css src/index.css
git commit -m "feat: scaffold team lunch poll app"
```

Expected: commit succeeds and contains only the scaffold files from this task.

---

### Task 2: Document App Commands in README

**Files:**
- Modify: `README.md`

**Interfaces:**
- Consumes: npm scripts `dev` and `check` from `package.json` created in Task 1.
- Produces: contributor-facing documentation for installing dependencies, running the local app, and running checks.

- [ ] **Step 1: Add a local development section to README**

Edit `README.md` so the file becomes exactly:

````markdown
# Team Lunch Poll

Team Lunch Poll is a small greenfield demo app for trying Patchmill on a safe,
disposable repository.

The app helps a team create a lunch poll, add meal or restaurant options, vote
on those options, and see the current winner. The repository intentionally
starts from seeded issues and grows feature by feature.

## Local development

Install dependencies:

```bash
npm install
```

Run the app locally:

```bash
npm run dev
```

Run checks before submitting changes:

```bash
npm run check
```

The check command runs TypeScript validation and a production build.

## Demo workflow

After this repository is seeded, try Patchmill manually:

```bash
patchmill init
patchmill triage --dry-run
patchmill triage
```

## Source of truth

This repository is disposable. The reusable project brief and issue prompts live
in the Patchmill package under `fixtures/patchmill-test-repo/`.
````

- [ ] **Step 2: Verify README commands match package scripts**

Run:

```bash
node -e "const pkg=require('./package.json'); for (const script of ['dev','check']) { if (!pkg.scripts?.[script]) throw new Error('missing script: '+script); } console.log('README commands are backed by package scripts');"
```

Expected: command prints `README commands are backed by package scripts` and exits with code 0.

- [ ] **Step 3: Re-run the full check after documentation changes**

Run:

```bash
npm run check
```

Expected: TypeScript validation and Vite production build both complete successfully.

- [ ] **Step 4: Commit README documentation**

Run:

```bash
git add README.md
git commit -m "docs: document app scaffold commands"
```

Expected: commit succeeds and contains only `README.md`.

---

## Final Validation

After all tasks are implemented, run:

```bash
npm install
npm run check
npm run dev -- --host 127.0.0.1
```

Expected final state:

- `npm install` exits with code 0 and uses the committed `package-lock.json`.
- `npm run check` exits with code 0 after TypeScript validation and a production build.
- The local dev server starts without errors.
- Browser verification shows the `Team Lunch Poll` heading, product description, and visually distinct placeholder area for future poll creation and results.
- No backend service, authentication, deployment setup, persistence, voting, winner calculation, or poll input behavior is present.

## Self-Review Notes

- Spec coverage: Task 1 creates the TypeScript-friendly scaffold and static app shell; Task 2 documents install, local development, and check commands.
- Placeholder scan: The plan uses concrete placeholder UI copy required by the issue and contains concrete file contents and commands for each implementation step.
- Type consistency: `App` is defined as the default export in `src/App.tsx` and imported as the default export by `src/main.tsx`; README commands match `package.json` scripts.
